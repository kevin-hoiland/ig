Hello <%= @resource.email %>!

Someone has requested a link to change your Intl Gum password, and you can do this through the link below.

<%= link_to 'Change my password', edit_password_url(@resource, :reset_password_token => @resource.reset_password_token), :class => 'content_link_button' %>