<% title "Create New Account" %>
<% description "Location for creating a new Intl Gum account." %>
<%= currently_at 'Members' %>

<h1>Create Membership Account</h1>

<p>Welcome to Intl Gum!  Membership is free and always will be free.  We encrypt your info and always keep it private, also we won't spam your email.  If you love chewing gum, simply signup now to share your opinions and become a chewsy tsiklaphilias!</p>
<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div><%= f.label :email %><br />
  <%= f.email_field :email, :autofocus => true %></div>

  <div><%= f.label :password %><br />
  <%= f.password_field :password %></div>

  <div><%= f.label :password_confirmation %><br />
  <%= f.password_field :password_confirmation %></div>

  <div><%= f.submit "Sign up", :class => 'content_button' %></div>
<% end %>

<%= render "devise/shared/links" %>
